// Get the image card element
const imageCard = document.getElementById("image-card");

// Add a click event listener to the image card
imageCard.addEventListener("click", function() {
  // Redirect to a new HTML page when the image card is clicked
  window.location.href = "path-to-your-new-html-page.html";
});
